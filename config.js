/* ---------------------------------------------------------------------------
   Mapdash configuration.

   Mapdash runs entirely in the browser. There are no accounts, no sign-ups and
   no server: your progress lives in this browser's local storage and nowhere
   else. Leaving these blank is the supported setup and keeps it that way.
--------------------------------------------------------------------------- */
window.MAPDASH_CONFIG = {
  supabaseUrl:     '',
  supabaseAnonKey: ''
};
